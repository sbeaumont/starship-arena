# Glossary

The words the code uses. Using different ones in a UI, an API field or a commit message is how
vocabulary drifts.

**Term.** What it means here. If two words could mean the same thing, say which one wins and that
the other is never used.
