{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_response.filePath // .tool_input.file_path // empty' | { read -r f; [ -n \"$f\" ] && cd \"${CLAUDE_PROJECT_DIR:-.}\" && uv run --group test python -m test.docs.check_file \"$f\"; }",
            "statusMessage": "Checking the file against the invariants",
            "timeout": 30
          }
        ]
      }
    ]
  }
}