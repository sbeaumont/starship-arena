#!/usr/bin/env bash
# Svelte game UI dev server (Vite), with hot module reload: http://localhost:5173
# Calls to /api are proxied to the FastAPI server, so run arena-api.sh alongside this.
npm run dev --prefix game-ui